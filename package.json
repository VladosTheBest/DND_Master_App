{
  "name": "shadow-edge-gm-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @shadow-edge/web",
    "dev": "npm run dev --workspace @shadow-edge/web",
    "server": "go run ./apps/server/cmd/server",
    "reset-password": "go run ./apps/server/cmd/server reset-password",
    "test:go": "go test ./apps/server/..."
  },
  "dependencies": {
    "@openai/codex": "0.152.0",
    "sharp": "^0.35.3"
  }
}
